# Ansible Collection - ansi_colle.linux_config

All roles in this collection should be platform independent as possible.
Do not put any package manager task here: `ansible.builtin.dnf` & `ansible.builtin.apt`.
